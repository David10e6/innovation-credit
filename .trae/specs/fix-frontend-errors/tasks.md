# 前端错误修复 - 实现计划（分解和优先排序任务列表）

## [x] 任务 1：添加通知相关API接口
- **优先级**：P0
- **依赖**：None
- **描述**：
  - 在api/index.js中添加通知相关的API接口
  - 包括获取通知列表、发布通知、编辑通知、删除通知等接口
- **验收标准**：AC-4
- **测试要求**：
  - `programmatic` TR-1.1：API接口定义正确，路径与后端一致
  - `programmatic` TR-1.2：API调用参数与后端对齐
- **备注**：需要根据后端API的具体结构调整接口定义

## [x] 任务 2：修复学生端SchoolNotice.vue
- **优先级**：P0
- **依赖**：任务 1
- **描述**：
  - 删除本地假数据
  - 调用真实API获取通知数据
  - 自动携带学生的院系ID
  - 只显示本院和校级通知
  - 移除所有操作按钮，只保留查看功能
  - 正确绑定接口返回的rows列表和total总数
- **验收标准**：AC-1, AC-4, AC-6
- **测试要求**：
  - `programmatic` TR-2.1：学生端只显示本院和校级通知
  - `programmatic` TR-2.2：学生端无操作按钮
  - `programmatic` TR-2.3：数据正确加载和渲染
- **备注**：需要确保学生用户信息中包含院系ID

## [x] 任务 3：修复辅导员端NoticeManagement.vue
- **优先级**：P0
- **依赖**：任务 1
- **描述**：
  - 列表接口请求时自动携带当前辅导员的院系ID
  - 只查询本院通知
  - 发布通知时自动填充本院ID
  - 参数和后端完全对齐
  - 修复列表赋值和分页逻辑
  - 确保数据正常显示
  - 只展示本院通知的增删改查按钮
- **验收标准**：AC-2, AC-4, AC-5, AC-6, AC-7
- **测试要求**：
  - `programmatic` TR-3.1：辅导员端只显示本院通知
  - `programmatic` TR-3.2：发布通知时自动填充本院ID
  - `programmatic` TR-3.3：数据正确加载和渲染
  - `human-judgment` TR-3.4：只显示本院通知的操作按钮
- **备注**：需要确保辅导员用户信息中包含院系ID

## [x] 任务 4：修复管理员端NoticeManagement.vue
- **优先级**：P0
- **依赖**：任务 1
- **描述**：
  - 发布接口添加校级通知标识
  - 请求查询全部校级通知
  - 修复数据绑定和分页逻辑
  - 正常展示数据库里的校级通知
  - 只保留校级通知的增删改查操作
- **验收标准**：AC-3, AC-4, AC-5, AC-6, AC-7
- **测试要求**：
  - `programmatic` TR-4.1：管理员端只显示校级通知
  - `programmatic` TR-4.2：发布通知时自动添加校级标识
  - `programmatic` TR-4.3：数据正确加载和渲染
  - `human-judgment` TR-4.4：只显示校级通知的操作按钮
- **备注**：需要确定校级通知的标识字段

## [x] 任务 5：统一三端API调用
- **优先级**：P1
- **依赖**：任务 1, 2, 3, 4
- **描述**：
  - 统一三端API路径、请求方式、参数字段
  - 确保与后端完全一致
  - 完善错误处理和提示
- **验收标准**：AC-4
- **测试要求**：
  - `programmatic` TR-5.1：API调用参数与后端对齐
  - `programmatic` TR-5.2：错误处理完善，提示明确
- **备注**：需要与后端API文档对齐

## [x] 任务 6：修复分页参数
- **优先级**：P1
- **依赖**：任务 2, 3, 4
- **描述**：
  - 修复分页参数pageNum、pageSize和后端对齐
  - 确保分页功能正常工作
- **验收标准**：AC-6
- **测试要求**：
  - `programmatic` TR-6.1：分页参数与后端一致
  - `programmatic` TR-6.2：分页功能正常，数据正确加载
- **备注**：需要确认后端分页参数的具体名称

## [x] 任务 7：完善表单校验
- **优先级**：P1
- **依赖**：任务 3, 4
- **描述**：
  - 完善通知发布表单的校验
  - 确保发布参数完整合法
  - 提供清晰的错误提示
- **验收标准**：AC-5
- **测试要求**：
  - `programmatic` TR-7.1：表单校验规则完整
  - `programmatic` TR-7.2：错误提示清晰明确
- **备注**：需要根据后端要求设置校验规则

## [x] 任务 8：测试和验证
- **优先级**：P2
- **依赖**：所有任务
- **描述**：
  - 测试三端的功能是否正常
  - 验证权限控制是否正确
  - 确保数据安全和功能完整性
- **验收标准**：所有AC
- **测试要求**：
  - `programmatic` TR-8.1：所有功能正常运行
  - `human-judgment` TR-8.2：用户体验良好，界面响应及时
- **备注**：需要在不同角色下测试系统功能